import tensorflow as tf
from model import ResNet101
from glob import glob


if __name__ == "__main__":

    dataset_dir = ""

    dataset = ""

    input_pipeline(dataset, batch_size=100, num_preprocess_threads=4, num_gpus=3):

    #logits = ResNet101(img)
